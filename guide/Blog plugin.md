The blog plugin should enable a timeline of posts, but implement it using backlinks as much as possible for simplicity (see [[Markdown notes can be extended without compromising simplicity]]).

- [ ] Create the plugin, traverse backlinks and include a timeline.