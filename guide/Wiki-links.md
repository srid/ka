A wiki-link is wrapped in double-square brackets, similar to how they appear on the site Wikipedia.

The following:

```markdown
This is [a link](./a%20link.md)
```

is equivalent to:

```markdown
This is [[a link]]
```

Wiki-links are most convenient to use when using [[Filenames as title]].